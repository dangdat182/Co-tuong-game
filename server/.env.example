# Copy file này thành .env và điền giá trị thật
# cp .env.example .env

# JWT secret key (đặt chuỗi ngẫu nhiên dài, tối thiểu 32 ký tự)
JWT_SECRET=your_random_secret_key_here

# Port server (mặc định 3001)
PORT=3001
